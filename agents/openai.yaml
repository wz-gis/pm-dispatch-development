interface:
  display_name: "PM 调度式开发"
  short_description: "结构化任务命名、Worker调度、证据与自动Gate校验"
  default_prompt: "使用 $pm-dispatch-development 将这个需求命名为类似 BUG-041 P1 AA 最近诊断记录的任务，声明通用能力和模型请求，通过 Adapter Resolver 选择 Provider，维护 Task/Evidence，并通过自动 Gate 收口。"
